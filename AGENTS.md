# 仓库协作约定

## 通用规则

1. 回复一律使用中文。
2. 生成的非代码类文件一律使用中文编写。
3. 代码中的注释一律使用中文编写。
4. 生成表格类文件时，优先使用 `xlsx` 格式。

## 项目定位

1. 当前项目为“邮箱账号管理系统”。
2. 项目定位是单管理员后台，不做前台注册，不做多角色。
3. 管理员登录方式固定为“用户名 + 密码”。
4. 管理员账号由数据库预置，密码只允许使用哈希存储。

## 技术与目录约定

1. 默认技术栈为 `Next.js + TypeScript + Supabase + Vercel`。
2. 前端路由使用 App Router。
3. 页面与接口代码放在 `src` 目录下。
4. 业务逻辑与数据处理工具放在 `src/lib`。
5. 数据库结构、初始化脚本、迁移脚本放在 `supabase` 目录下。
6. 需求方案、实施计划、数据库设计等文档放在 `docs` 目录下。

## 当前功能约定

1. 登录页只保留登录框区域，不保留额外的左侧介绍区。
2. 主页面为列表页，支持搜索、筛选、分页、新增、编辑、软删除。
3. 新增和编辑统一使用弹窗表单。
4. 删除默认采用软删除，除非用户明确要求物理删除。
5. 顶部统计区显示：
   - 未关联数量
   - 已关联数量
   - 失效邮箱占比
   - 关联邮箱平均存活时长
6. 首页列表当前显示列为：
   - 邮箱账号名称（含复制按钮）
   - 注册时间
   - 注册时长
   - 注册地点
   - 是否关联 s2a
   - 关联时间
   - 操作

## HeroSMS 模块约定

1. HeroSMS 为独立模块，只复用现有管理员登录入口和首页入口，不复用 `email_accounts` 相关表、接口和页面逻辑。
2. HeroSMS 的服务端接入代码统一放在 `src/lib/hero-sms`，页面与接口放在 `src/app/dashboard/hero-sms` 和 `src/app/api/hero-sms`。
3. HeroSMS 购买、状态、生命周期动作优先以官方接口真实返回为准，不得凭经验硬编码固定有效时长、固定状态流或固定价格规则。
4. HeroSMS 相关接口说明优先参考仓库内文档 `docs/herosms-api-cn.json` 以及官网文档，不在 `AGENTS.md` 中直接维护大段接口细节。
5. HeroSMS 的 API Key 只允许通过环境变量提供，不得写入仓库跟踪文件、测试样例或文档正文。
6. HeroSMS webhook 路由固定为 `POST /api/hero-sms/webhook`，当前不再依赖 `secret` 或来源 IP 白名单，而是仅在本地存在活动中的 HeroSMS 号码时才接受回调，并要求 payload 中的 `activationId` 能匹配到当前活动记录。
7. HeroSMS webhook 只负责写入短信内容与验证码，不在 webhook 到达时直接结束活动记录。
8. 对外部程序开放的内部接口优先复用 `Bearer Token` 方案，当前已存在 `POST /api/internal/hme-ingest` 和 `GET /api/internal/hero-sms-active` 两类示例。

## 数据字段约定

1. `email_accounts.email_name` 为必填字段。
2. 除 `email_name` 外，其他业务字段允许为空。
3. `registered_at`、`linked_at`、`expired_at` 当前在前端按“日期”处理，不带时分，目的是避免时区偏移。
4. `birthday` 也是日期字段。
5. `is_linked_s2a = false` 时，`linked_at` 应为空。
6. `is_expired = false` 时，`expired_at` 应为空。
7. `email_accounts.is_plus` 用于区分 free/plus 类型，且为必填布尔字段。

## 列表与排序约定

1. 列表默认每页显示 `10` 条。
2. 列表默认排序规则为：
   - 先按 `is_linked_s2a`，未关联在前，已关联在后
   - 再按 `registered_at` 升序，日期早的在前，空值在后
   - 再按 `created_at` 升序，创建早的在前
3. 顶部统计卡片基于“全部有效记录”计算，不随下方筛选结果变化。
4. 列表、统计、域名下拉、分页均按当前类型（free/plus）隔离。

## 邮箱账号输入约定

1. 邮箱账号名称在前端拆成“账号名称 + 邮箱域名”输入，但写库时仍合并保存到 `email_name`。
2. 域名下拉按当前类型（free/plus）的有效记录自动聚合，不受固定预设白名单限制。
3. 域名选择“自定义”时，允许用户输入新域名；一旦新增成功，后续会自动出现在该类型的域名下拉中。
4. 当用户在账号名称中粘贴完整邮箱时，前端会自动拆分账号名和域名，并自动匹配或回填到域名输入区域。

## 类型分流约定

1. 首页默认显示 free，可切换 plus。
2. free/plus 切换影响列表、统计、域名选项、分页与搜索结果。
3. 新增与编辑时必须明确选择邮箱类型（free 或 plus）。

## 系统设置约定

1. 全局可编辑人民币数值存放在 `system_settings.cny_price`（单例记录 `id = 1`）。
2. 首页右上角展示该数值，点击后可修改。
3. 这类全局单值配置不得混入 `email_accounts` 明细表。

## 开发约定

1. 优先保持实现简单直接，不为当前版本提前引入导出、审计日志、复杂权限等未确认需求。
2. 任何环境变量、密钥、账号密码都不得写入仓库跟踪文件。
3. 涉及数据库约束变化时，除了更新 `schema.sql`，还应补对应迁移脚本，便于在已有 Supabase 项目中执行。
4. 涉及可见行为变更时，优先补测试或更新已有测试。

## 文档约定

1. 长期稳定的协作规则放在 `AGENTS.md`。
2. 会变化的产品需求与实施方案放在 `docs/plans`。
3. 启动、部署、环境变量说明放在 `README.md`。

## 不应写入 AGENTS.md 的内容

1. 管理员用户名和密码。
2. Supabase 密钥、Vercel 密钥、会话密钥等敏感信息。
3. 一次性的临时任务说明。
4. 明显可能频繁变动的短期产品细节。
